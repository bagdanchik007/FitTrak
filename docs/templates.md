# Workout templates

Templates store planned exercise sequences (sets/reps targets).

API:

- `POST /api/v1/templates`
- `GET /api/v1/templates`
- `GET /api/v1/templates/{id}`
- `DELETE /api/v1/templates/{id}`

Domain helpers live in `app/domain/template/`.
